



Рекурсии
Вызов функции внутри самой функции.

n! = 1*2*3* .. * n

n! = (n-1)!*n

5! = 4! * 5


factorial(n) = factorial(n-1) * n


Строки
// Объявление строки
String назвПеременной;

String str;

// Инициализация строки
str = "Привет";
str = new String("Привет");

int a = 1, b = 1;
if(a == b) // true

String str1 = "Привет";

String str2 = "Строка";

if(str1 == str2) // не можем так сравнивать строки по их содержимому (т.к. сравнивается по их ссылке)


if(str1.equals(str2)) // сравнение содержимой строки

Задача
Пользователь вводит Фамилия Имя Отчество, Фамилия И. Р.\

char charAt(int index) // Возвращает символ из строки по индексу

String name = "Имя";

name.charAt(0); // Функция вернёт 'И'

substring(int begin, int end); // Срезает и возвращает строку от индекса begin до end

Написать программу которая принимает в качестве аргумента номер телефона и выдаёт его частично скрытую форму
Пример
+79993213232
********3232
89993213232
*******3232

Получение длины строки str.length() 